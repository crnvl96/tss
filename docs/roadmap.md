```
schema  →  chunker  →  embedding helper  →  /search (with seed data)  →  /ingest + Temporal
                                                                        └──────────────────┘
                                                                                ↓
                                                           replaces "hello world" route tests with real ones
```
